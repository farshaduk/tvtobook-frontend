'use client'

// This component is deprecated. Use ProtectedRoute from './ProtectedRoute' instead
// Kept for backward compatibility only

import { ProtectedAdminRoute as NewProtectedAdminRoute } from './ProtectedRoute';

export const ProtectedAdminRoute = NewProtectedAdminRoute;

